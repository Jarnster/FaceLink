Also works well on Windows!

1) Create a Venv

2) Activate Venv

3) > with venv activated, run:

pip install face_recognition requests opencv-python

pip install git+https://github.com/ageitgey/face_recognition_models

pip install --upgrade setuptools

4) Run:

python main.py
